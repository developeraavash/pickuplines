import 'package:flutter/material.dart';

class EyeExercises extends StatelessWidget {
  const EyeExercises({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('  exercises to strengthen vision.'),
    );
  }
}
