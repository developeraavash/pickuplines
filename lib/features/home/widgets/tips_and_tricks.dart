import 'package:flutter/material.dart';

class TipsAndTricks extends StatelessWidget {
  const TipsAndTricks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('  exercises to strengthen vision.'),
    );
  }
}
